name: Deploy (Vercel)

# Stack: Next.js / Vite / SPA hosted on Vercel.
# Triggers only POST-merge on main (deploy never runs on a PR).
#
# Required secrets (gh secret set ... -R {{REPO}}):
#   VERCEL_TOKEN       - Vercel account token
#   VERCEL_ORG_ID      - org id (vercel project ls / .vercel/project.json)
#   VERCEL_PROJECT_ID  - project id

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'app/**'
      - 'public/**'
      - 'package.json'
      - 'next.config.*'
      - 'vite.config.*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Vercel production
    runs-on: ubuntu-latest
    if: github.repository == '{{REPO}}'
    steps:
      - uses: actions/checkout@v5

      - name: Install Vercel CLI
        run: npm i -g vercel@latest

      - name: Pull env + build
        run: |
          vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
          vercel build --prod --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Deploy prebuilt
        run: vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
